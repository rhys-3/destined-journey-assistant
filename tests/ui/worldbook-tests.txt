(async () => {
  const results = [];
  const check = (name, ok) => { if (!ok) throw new Error(name); results.push(name); };
  const pause = ms => new Promise(resolve => setTimeout(resolve, ms));
  const q = selector => ui.shadow.querySelector(selector);
  window.ctx = { characterId: '角色A', chatId: '聊天A', groupId: '' };
  SillyTavern.getContext = () => ctx;
  window.bound = ['命定之诗'];
  window.books = {};
  window.readCount = 0; window.writeCount = 0;
  window.getCharWorldbookNames = () => ({ primary: bound[0] ?? null, additional: bound.slice(1) });
  window.getGlobalWorldbookNames = () => [];
  window.getChatWorldbookName = () => null;
  const names = ['output_format (随AI输出开，主API)', '[mvu_update]output_format_(使用额外模型更新变量开)', '[mvu_update]用户最新输入(使用额外模型更新变量开)'];
  const fixture = (main, extra, input) => [...[main, extra, input].map((enabled, i) => ({ uid: i + 1, name: names[i], enabled, content: '正文不得改动' + i, strategy: { type: 'constant' }, extra: { keep: true } })), { uid: 99, name: '世界书事件链', enabled: false, content: '不应自动处理' }];
  const reset = (main, extra, input) => { bound = ['命定之诗']; books = { 命定之诗: fixture(main, extra, input) }; };
  const localMode = mode => {
    const ids = ui.GROUPS['variable-mode'].options.map(([id]) => id);
    for (const preset of [data, stored, ui.state.preset]) ids.forEach((id, i) => { preset.prompts.find(p => p.id === id).enabled = i === (mode === 'main' ? 0 : 1); });
  };
  window.getWorldbook = async name => { readCount++; if (window.readGate) await window.readGate; if (window.readFailure === name) throw Error('模拟读取失败'); return structuredClone(books[name]); };
  window.updateWorldbookWith = async (name, updater) => { writeCount++; if (window.writeGate) await window.writeGate; if (window.writeFailure === name) throw Error('模拟保存失败'); books[name] = await updater(structuredClone(books[name])); ui.scheduleWorldbookScan(); return structuredClone(books[name]); };
  ui.state.activeTab = 'daily'; ui.render();
  reset(true, false, false); localMode('extra'); await ui.scanWorldbookMode();
  check('世界书主 API 自动匹配预设', ui.variablePresetMode() === 'main');
  check('自动识别只改预设，不改世界书', writeCount === 0);
  reset(false, true, true); localMode('main'); await ui.scanWorldbookMode();
  check('世界书额外 API 自动匹配预设', ui.variablePresetMode() === 'extra');
  const untouched = JSON.stringify(books['命定之诗'].map(({ enabled, ...entry }) => entry));
  await ui.selectVariableMode('main');
  check('主 API 三条目互斥联动', JSON.stringify(books['命定之诗'].slice(0, 3).map(e => e.enabled)) === '[true,false,false]');
  await ui.selectVariableMode('extra');
  check('额外 API 三条目互斥联动', JSON.stringify(books['命定之诗'].slice(0, 3).map(e => e.enabled)) === '[false,true,true]');
  check('仅修改启用状态且事件链未被自动开启', JSON.stringify(books['命定之诗'].map(({ enabled, ...entry }) => entry)) === untouched && !books['命定之诗'][3].enabled);
  check('额外 API 显示正确 MVU 非弹窗提醒', q('.mvu-reminder').textContent.includes('额外模型解析'));
  await ui.selectVariableMode('main'); check('主 API 显示正确 MVU 提醒', q('.mvu-reminder').textContent.includes('随 AI 输出'));
  reset(false, false, false); localMode('main'); await ui.scanWorldbookMode();
  check('全部关闭时保留选择并提示', ui.variablePresetMode() === 'main' && ui.worldLink.phase === 'warning');
  reset(true, true, true); localMode('extra'); await ui.scanWorldbookMode();
  check('两种输出混开不擅自决定', ui.variablePresetMode() === 'extra' && ui.worldLink.phase === 'warning');
  await ui.selectVariableMode('main'); check('手动选择能修复混开', books['命定之诗'].slice(0, 3).filter(e => e.enabled).length === 1);
  reset(false, true, false); localMode('main'); await ui.scanWorldbookMode();
  check('明确额外输出但漏开输入时识别并指出', ui.variablePresetMode() === 'extra' && ui.worldLink.books[0].issue.includes('用户最新输入未开'));
  await ui.selectVariableMode('extra'); check('重选当前模式补开遗漏输入', books['命定之诗'][2].enabled);
  reset(true, false, true); await ui.scanWorldbookMode();
  check('主 API 残留额外输入被指出', ui.worldLink.books[0].issue.includes('仍开着'));
  reset(true, false, false); books['命定之诗'].splice(2, 1); localMode('main'); await ui.scanWorldbookMode();
  check('缺少条目不猜测自动模式', ui.variablePresetMode() === 'main' && ui.worldLink.phase === 'warning');
  await ui.selectVariableMode('extra'); check('缺条目仍切换已存在项而不捏造条目', books['命定之诗'].length === 3 && books['命定之诗'][1].enabled && ui.worldLink.books[0].missing.includes('input'));
  reset(true, false, false); books['命定之诗'].push({ ...books['命定之诗'][0], uid: 20 }); const oldWrites = writeCount; await ui.selectVariableMode('extra');
  check('重复名称停止世界书写入', writeCount === oldWrites && ui.worldLink.books[0].duplicate.includes('main'));
  bound = []; await ui.selectVariableMode('main');
  check('未绑定世界书仍可保存预设', ui.variablePresetMode() === 'main' && ui.worldLink.message.includes('尚未联动'));
  reset(true, false, false); window.readFailure = '命定之诗'; await ui.selectVariableMode('extra');
  check('读取失败不会阻止预设模式保存', ui.variablePresetMode() === 'extra' && ui.worldLink.issues.some(s => s.includes('模拟读取失败'))); window.readFailure = null;
  reset(true, false, false); window.writeFailure = '命定之诗'; await ui.selectVariableMode('extra');
  check('保存失败明确报告部分未完成', ui.variablePresetMode() === 'extra' && books['命定之诗'][0].enabled && ui.worldLink.issues.some(s => s.includes('模拟保存失败'))); window.writeFailure = null;
  reset(true, false, false); bound.push('另一绑定世界书'); books['另一绑定世界书'] = fixture(false, true, true); localMode('extra'); await ui.scanWorldbookMode();
  check('多本世界书冲突不自动跟随', ui.variablePresetMode() === 'extra' && ui.worldLink.phase === 'warning');
  await ui.selectVariableMode('main'); check('手动模式统一所有已绑定相关世界书', Object.values(books).every(book => book[0].enabled && !book[1].enabled && !book[2].enabled));
  const counts = {};
  for (const tab of ['daily', 'style', 'tools']) { ui.state.activeTab = tab; ui.render(); for (const input of ui.shadow.querySelectorAll('input[data-key^="prompt:"]')) counts[input.dataset.key] = (counts[input.dataset.key] || 0) + 1; }
  check('三个主页面无重复提示词开关', Object.values(counts).every(n => n === 1));
  check('事件链只出现一次', counts['prompt:' + ui.IDS.eventChain] === 1);
  check('数值和人称规则仍有启停能力', [ui.IDS.dialogue, ui.IDS.outputLength, ui.IDS.narration].every(id => counts['prompt:' + id] === 1));
  ui.state.activeTab = 'advanced'; ui.render(); check('全部条目页不再重复开关', ui.shadow.querySelectorAll('input[data-key^="prompt:"]').length === 0);
  q('[data-action="prompt-open"][data-id="' + ui.IDS.eventChain + '"]').click(); check('条目列表打开独立只读编辑器', !!q('.prompt-editor') && q('[data-field="content"]').readOnly); ui.closePromptEditor(); ui.state.activeTab='daily'; ui.render();
  reset(true, false, false); const readsBefore = readCount; for (let i = 0; i < 30; i++) ui.scheduleWorldbookScan(); await pause(650);
  check('连续事件合并成一次检查', readCount - readsBefore === 1);
  const quietReads = readCount; await pause(1700); check('空闲不持续读取世界书', readCount === quietReads);
  reset(true, false, false); let releaseRead; window.readGate = new Promise(r => { releaseRead = r; }); const timeoutScan = ui.scanWorldbookMode(); await timeoutScan;
  check('世界书读取超时可返回', ui.worldLink.phase === 'warning' && ui.worldLink.issues.some(s => s.includes('超时')));
  const pendingReads = readCount; const rescan = ui.scanWorldbookMode(); await pause(30); check('超时后复查复用原请求不堆积', readCount === pendingReads); window.readGate = null; releaseRead(); await rescan;
  reset(true, false, false); let releaseWrite; window.writeGate = new Promise(r => { releaseWrite = r; }); await ui.selectVariableMode('extra');
  check('写入超时返回界面并标注待确认', ui.worldWrites.size === 1 && !ui.worldLink.busy && !!q('.inline-warning'));
  await ui.setNumericField('hanzi', '1800'); check('世界书写入待确认不堵塞其他设置保存', vars.managed_values.min_hanzi === '1800');
  const blockedWrites = writeCount; await ui.selectVariableMode('main'); check('未确认写入期间不重复提交切换', writeCount === blockedWrites);
  window.writeGate = null; releaseWrite(); await pause(650); check('超时请求未进入更新器时阻止迟到写入', books['命定之诗'][0].enabled && ui.worldWrites.size === 0);
  reset(true, false, false); window.readGate = new Promise(r => { releaseRead = r; }); const stale = ui.scanWorldbookMode(); await pause(30); ctx.chatId = '聊天B'; ui.scheduleWorldbookScan(); localMode('extra'); window.readGate = null; releaseRead(); await stale;
  check('切换聊天后丢弃旧读取结果', ui.variablePresetMode() === 'extra'); await pause(650);
  reset(true, false, false); window.writeGate = new Promise(r => { releaseWrite = r; }); const staleWrite = ui.selectVariableMode('extra'); await pause(30); ctx.chatId = '聊天C'; ui.scheduleWorldbookScan(); window.writeGate = null; releaseWrite(); await staleWrite;
  check('更新器提交前再次确认上下文', books['命定之诗'][0].enabled && !books['命定之诗'][1].enabled); await pause(650);
  reset(true, false, false); await ui.scanWorldbookMode(); await ui.setNumericField('hanzi', '1500'); ui.state.activeTab = 'daily'; ui.render();
  reset(false, true, true); localMode('main'); ui.closePanel();
  for (const fn of handlers.get('WORLDINFO_UPDATED')) fn('命定之诗', {}); await pause(650);
  check('真实启动注册的世界书事件在面板关闭时也可同步', ui.variablePresetMode() === 'extra');
  const eventReads = readCount; for (const fn of handlers.get('WORLDINFO_UPDATED')) fn('无关世界书', {}); await pause(450);
  check('无关世界书事件不触发扫描', readCount === eventReads);
  ui.openPanel(); await pause(650); reset(true, false, false);
  window.writeGate = new Promise(r => { releaseWrite = r; }); const concurrent = ui.selectVariableMode('extra'); await pause(30);
  books['命定之诗'][0].content = '其他编辑刚修改的正文'; books['命定之诗'][3].content = '其他编辑修改事件条目';
  window.writeGate = null; releaseWrite(); await concurrent;
  check('联动保留更新器收到的最新正文与无关条目', books['命定之诗'][0].content === '其他编辑刚修改的正文' && books['命定之诗'][3].content === '其他编辑修改事件条目');
  reset(true, false, false); window.writeGate = new Promise(r => { releaseWrite = r; }); const conflictingEdit = ui.selectVariableMode('extra'); await pause(30);
  books['命定之诗'][0].enabled = false; window.writeGate = null; releaseWrite(); await conflictingEdit;
  check('读取后条目开关被外部修改时拒绝覆盖', !books['命定之诗'][1].enabled && ui.worldLink.issues.some(s => s.includes('其他操作修改')));
  reset(true, false, false); await ui.scanWorldbookMode(); ui.state.activeTab = 'daily'; ui.render();
  return results;
})()
