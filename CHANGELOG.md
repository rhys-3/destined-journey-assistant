# 更新日志

## 未发布

## 3.4.3

- 优化条目搜索范围：默认只搜条目名，可多选正文、简介和 ID；范围切换立即更新结果，保留输入框与关键词，并在本次会话内保留选择。显示当前范围与多关键词用法，至少保留一个范围，避免无意匹配全部正文。

## 3.4.2

- 修复预设条目搜索时重建输入框打断中文拼音输入的问题；组合输入完成后刷新结果，保留输入焦点。
- 条目搜索覆盖名称、完整正文、界面简介和 ID，支持空格分隔多关键词跨字段匹配，忽略大小写与全半角差异；显示当前筛选范围的匹配数量，保留发送顺序和排序保护。

## 3.4.1

- 修复移动端轻触悬浮球时，后续点击误中面板关闭按钮而立即关闭的问题；悬浮球无需挪动即可正常打开，下一次主动点击关闭不受影响。

## 3.4.0

- 预设 1.4 起 NSFW 板块改用 `nsfw-mode`／`nsfw-extra` 标识；助手同步更新分组、布局、样式类与界面文案，并在读取时把 1.3.1 及更早的 `adult-mode`／`adult-extra` 归一化到当前标识，迁移作者布局，旧预设仍可正常显示与保存。
- 兼容旧 NSFW 板块中缺失或为空的单选组字段：四个内置模式恢复为选择卡，点击后互斥保存；已同时开启多个模式时显示待修正状态。
- NSFW 模式与附加描写在「文风与表达」中合为一个可折叠区域，收起时显示当前模式与附加项数量；四个模式使用双列选择卡，三个附加项独立开关，切换模式保留附加选择。
- 识别防发情模式、SFW 描写、亲密节奏及描写约束，提供可编辑简介；保护 NSFW 板块起止条目，兼容旧版三模式和自定义显示位置。

## 3.3.0

- 新增「自定义设定」分类，全局设定与用户附加设定支持逐条添加、启停、排序、删除撤销及多行编辑；总开关关闭时仍保留内容与条目状态。
- 设定列表纳入命名配置、恢复点及导入导出，自动迁移旧文本与标准提示词，保留已有新版数据、自定义内容及旧宏兼容；完善输入、保存失败与预设切换时的草稿保护。
- 「文风与偏好」改为「文风与表达」，「总结」改为「总结设置」，配置管理、界面外观与打开方式合并至「设置」。
- 收紧手机端顶部留白，分开关闭按钮与预设条目入口，并修复窄屏标题拆字换行。

## 3.2.0

- 表达约束显示为总开关，下方统一排列内置与自建细则；关闭时禁用子控件并保留选择，新增细则的发送位置默认在总开关条目前。
- 移除已停用的成人词汇条目适配，兼容不含旧内容偏好板块的预设布局。
- 新建普通条目支持选择独立开关或单选项，并按所在板块给出默认类型；保留已有选择、已有条目的单选关系，以及基调和主文风的专用创建流程。
- 条目编辑器支持界面简介，包含普通开关、单选项和数值／语言卡片；可实时预览、清空隐藏，并随条目复制、移动和预设配置导出保留。简介不加入提示词正文。

## 3.1.2

- 修复手机端总结条目编辑弹窗越出屏幕的问题；弹窗随可见区域调整，内容独立滚动，标题与操作按钮保持可见，打开时不再自动唤起键盘。

## 3.1.1

- 修复宏检查误删中文消息结构标签的问题；正文边界及「记忆区／参考区／运行规则区」交由配套预设脚本处理，未知宏仍会拦截。
- 配合命定·黄昏 Prime 1.1.1 使用；补充两个请求阶段的浏览器回归，并串联宏展开、消息整理与并发总结检查。

## 3.1.0

- 总结页面分为记录与任务、生成设置、提示词和世界书，统一后台进度、任务日志、保存反馈与主题弹窗。
- 支持自动、手动开始和指定楼层三种总结入口；提供摘要推荐方案与独立单批上限，一轮按完整可总结范围拆分请求。
- 批次默认串行，可设置并发数；失败结果进入待处理，支持编辑、重生成、跳过及继续未完成批次，保存或显隐失败可单独恢复。
- 普通总结与大总结改用可启停、排序和弹窗编辑的提示词条目，支持自然角色、自定义宏、展开预览与专用导入导出；默认使用时间地点档案，只保存有效结果标签内的正文。
- 正文提取与排除标签逐项管理，保留用户输入全文；大总结默认累计 15 条后合并最早连续 10 条，保留旧分段记忆及用户自定义设置。
- 楼层显隐支持按聊天开启自动隐藏、范围与类型操作；第 0 楼纳入计数，明细分页、筛选、按编号跳转并按需加载原文。
- 首次总结可自动创建独立世界书，完善绑定、迁移、解绑、删除和失效处理；来源变更会使对应记忆失效，仅恢复助手管理的隐藏楼层。
- 后台任务在关闭面板后继续运行；切换聊天、预设或卸载时取消旧任务，阻止迟到结果跨上下文写入，保留可恢复进度。
- 完善条目编辑和配置交互：保存成功自动关闭编辑窗，失败保留草稿，未修改的新条目可直接关闭。
- 总结请求直接按提示词条目的自然 role 发送，配套命定独立消息处理脚本；移除旧 SP 专用发送隔离，保留酒馆与模型接口自身的格式转换。
- 当前分支的软件许可调整为 PolyForm Noncommercial 1.0.0；更新加载器、构建标识和分发说明，保留历史 MIT 标签及第三方许可。

## 3.0.1

- 适配预设统一使用

- 预设设置按功能拆分为 ES 模块，保留原行为与配置身份。
- Actions 在测试通过后自动提交 dist，版本标签指向包含产物的提交。
- 通过 version:prepare 同步版本文件，使用 Git 标签分发，无需创建 Release。
- 每三天检查酒馆助手 @types 上游更新，仅提交内容变化。

## 3.0.0

- 仓库改名 destined-journey-assistant，沿用原历史和标签。
- 合并设置与总结，统一主题、弹窗、存储与生命周期。
- 总结页整合普通／大总结、世界书、参数、两套提示词。
- 一次迁移旧配置和启用状态，新装默认关闭。
- 配置保存／导出可选择预设和总结，分享格式 v2、兼容 v1。
- 修复整表覆盖、保存失败仍成功、迟到结果跨聊天保存、自动打开手动隐藏楼层。
- 单份 ES 模块源码、固定外链、先验证再发布，不覆盖标签。
- 冻结旧 dist 为 v2.8.3，保留旧固定标签与旧文件 main／latest 链接。

## 2.x 历史

2.x 提供独立聊天总结功能。代码、构建产物和版本文档见 v2.7.0、v2.8.2、v2.8.3 标签。
